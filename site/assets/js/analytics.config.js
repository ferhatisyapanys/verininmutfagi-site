// Analytics config
// Prefer values already defined on the page (e.g., in a separate config script).
// Leaving empty disables network calls.
window.ANALYTICS_URL = window.ANALYTICS_URL || '';
window.ANALYTICS_TOKEN = window.ANALYTICS_TOKEN || '';
